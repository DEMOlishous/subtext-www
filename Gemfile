source "https://rubygems.org"

gem "jekyll", "~> 4.3"
gem "jekyll-seo-tag", "~> 2.8"

# GitHub Pages compatibility (kept loose; if you switch to gh-pages strict
# remote build, swap in `gem "github-pages"` and remove the bare jekyll line).

group :jekyll_plugins do
  gem "jekyll-feed", "~> 0.17"
end
